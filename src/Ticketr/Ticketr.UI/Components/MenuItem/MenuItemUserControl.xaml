<UserControl x:Class="Ticketr.UI.Components.MenuItemUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ticketr.UI.Components"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="24" d:DesignWidth="170">
    <Button Height="24" Padding="0"  Background="Transparent" BorderBrush="Transparent">
        <Grid Width="170" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Rectangle Width="5" Fill="{DynamicResource AccentColorBrush}"></Rectangle>
            <materialDesign:PackIcon Kind="{Binding IconName}" Height="24" Width="24" Grid.Column="1" Margin="10,0,0,0"/>
            <TextBlock Text="{Binding Name}" Grid.Column="2" HorizontalAlignment="Center" Margin="10,0,0,0" VerticalAlignment="Center"></TextBlock>
        </Grid>
    </Button>
</UserControl>
